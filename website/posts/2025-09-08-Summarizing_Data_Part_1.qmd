---
title: "Summarizing Data Part 1"
author: "Jason Bryer"
date: 2025-09-08
draft: false
description: "Slides and recording for the *Summarizing Data Part 1* meetup."
categories: ["Meetups"]
image: "2025-09-08-Summarizing_Data_Part_1.png"
---


[Click here](/slides/02-Summarizing_Data1.html#1) to open the slides ([PDF](/slides/02-Summarizing_Data1.pdf)).

<iframe width="560" height="315" src="https://www.youtube.com/embed/dVGX-rAXFUQ" title="YouTube video player" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>

<pre>
x <- rnorm(500)

sample_var <- function(x) {
	sum( (x - mean(x))^2)  / (length(x) - 1)
}

pop_var <- function(x) {
	sum( (x - mean(x))^2)  / (length(x))
}

# var(x)
sample_var(x)
pop_var(x)

n <- 5:500

diff <- numeric(length(x))
for(i in n) {
	x2 <- x[1:i]
	diff[i] <- abs(sample_var(x2) - pop_var(x2))
}
diff

library(ggplot2)
ggplot(data.frame(x = 1:length(diff), y = diff), aes(x = x, y = y)) + geom_point()


# What position are the vowels?
letters
vowels <- c('a', 'e', 'i', 'o', 'u')
letters %in% vowels
letters %in% vowels |> which()
</pre>

